const prompt = require('prompt-sync')();

// /* 1 - Crie um programa que imprima 11 vezes uma frase*/

// let contador

// for(contador = 0 ; contador <= 11 ; contador++ )

//     console.log(`Ola ${contador}`)

// /* 2 - Escrever um programa para exibir os numero de um até 50*/

// let contador

// for(contador = 1; contador < 51; contador++)
// console.log(contador)

// /* 3 - Fazer o programa encontrar todos os numero pares de 1 até 100*/

// let contador, pares

// for(contador = 1; contador <= 100; contador++)
//     if(contador %2 == 0)
//     console.log(contador)


// /* 4 - Ler um valor inteiro(somente de 1 a 10) e escrever a tabuada*/

// let contador, resultado, numero

// numero = parseInt(prompt("Digite um numero até 10: "))

// for(contador = 1; contador <= 10; contador++) {
//     resultado = numero * contador
//     console.log(`${numero} x ${contador} = ${resultado}`)
// }


// /* 5 - Fazer o prgrama achar todos os numeros impares de 1 a 100*/

let contador

for(contador = 1; contador <= 100; contador++)
    if(contador%2 == 0){

    } else {
        console.log(contador)
    }